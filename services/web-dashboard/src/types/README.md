# TypeScript Type Definitions

Shared TypeScript interfaces and type definitions.

## Purpose
This directory contains type definitions used across the application:
- API response types
- Component prop types
- Domain models (Camera, Detection, Event, etc.)
